.bundle
.jekyll-metadata
_data/*
_report/
node_modules
build/
vendor
.idea
.DS_Store
.vscode
.*.swp
.wercker/*

# Antora tmp files
_site/

# Build caches (page date history clones and the page date map)
.cache/

# Cursor setup (local only, do not push)
.cursor/
AGENTS.md
.cursorignore

# Claude setup (local only, do not push)
.claude/
.claudeignore
CLAUDE.md
# .mcp.json
.mcp.json